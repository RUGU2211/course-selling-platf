spring:
  application:
    name: api-gateway
  # Config Server configuration is loaded from application.properties
  # Gateway routes are configured in config-server/src/main/resources/config/api-gateway.yml
  # This file should not contain gateway routes to avoid conflicts
